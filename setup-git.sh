#!/bin/bash
git config --global user.name "achristmaschi"
git config --global user.email "chitg.tran@gmail.com"
echo "✅ Git configured: $(git config --global user.name) <$(git config --global user.email)>"
